- defaults:
    link:
      relink: true

- link:
    ~/.zshrc:
      force: true
      path: zshrc
    ~/.ssh/config:
      create: true
      path: ssh/config
    ~/.gitconfig:
      force: true
      path: gitconfig
